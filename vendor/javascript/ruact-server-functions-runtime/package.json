{
  "name": "ruact-server-functions-runtime",
  "version": "0.1.0",
  "description": "Server-functions runtime for ruact gem (Story 8.1). Provides `_makeRef(name)` that POSTs to `/__ruact/fn/:name` with CSRF + JSON / FormData support.",
  "type": "module",
  "main": "index.js",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "default": "./index.js"
    }
  },
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
