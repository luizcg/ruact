// Story 10.2 (Task 5) — minimal stub for the relative `./<Model>DeleteDialog`
// import the generated List makes. 10.2 does NOT change DeleteDialog (10.4 owns
// its shadcn upgrade); this only needs to satisfy the import + the `<singular>`
// prop shape (`{ id: number }`) the List passes a row to.
export function PostDeleteDialog(_props: { post: { id: number } }): any {
  return null;
}
